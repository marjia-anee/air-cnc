import React from 'react';
import Header from '../Header/Header';

const NotFound = () => {
      return (
            <div>
                  <Header></Header>
                  <h1 className  = "text-center mt-5">This page is not found</h1>
            </div>
      );
};

export default NotFound;